"""Domain layer — pure business logic, zero framework dependencies.

Sub-packages:
- user/       — User aggregate
- organization/ — Organization/tenant aggregate
- role/       — Role and Permission entities
- document/   — Document aggregate
- conversation/ — Conversation and Message aggregates
- api_key/    — API key aggregate
- audit/      — Audit log entity
- embedding/  — Embedding placeholder (future RAG)
"""
